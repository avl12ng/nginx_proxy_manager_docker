# =================================================================
# Nginx Proxy Manager Automator Configuration
# =================================================================

# --- Shared Network Configuration ---
# This is the Docker bridge network shared between NPM and your apps
PROXY_NETWORK="nginx-proxy-network"

# --- Services to Link ---
# List the EXACT container names of your applications to be proxied.
# NPM will be able to reach them using these names.
CONTAINERS_TO_LINK=(
    "container_name1"
    "container_name2"
)

# --- Host Port Configuration ---
# These are the ports exposed on your physical server.
# Change them if you have other services already using these ports.
HTTP_PORT=80
HTTPS_PORT=443
ADMIN_PORT=81

# --- Custom SSL Certificate Path ---
# The absolute path on your HOST machine where your SSL certificates are stored.
# This folder MUST contain 'fullchain.pem' and 'privkey.pem'.
# Inside the container, this will be mapped to /data/custom_ssl/npm-1
SSL_CERT_PATH="/data/ssl/domain.org"

# --- Docker Compose Settings ---
# The filename of your Nginx Proxy Manager compose file
COMPOSE_FILE="docker-compose.yml"
